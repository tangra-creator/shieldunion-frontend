{
  "name": "shieldunion-platform",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build --prefix frontend && cp -r frontend/dist ./dist"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
